# Capstone — Dynamic Pricing for Perishable Inventory
# Tested on Python 3.11 (works on 3.10 and 3.12 too).

# Core data stack
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.10

# ML
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
statsmodels>=0.14
prophet>=1.1.5

# Plotting (used by sanity checks; dashboard uses Plotly)
matplotlib>=3.7

# Dashboard (Phase 2 — installed even though run_pipeline.py doesn't import it)
streamlit>=1.30
plotly>=5.18
